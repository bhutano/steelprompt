[steelprompt] Before responding, evaluate this prompt:

If critical information is missing that would significantly change the result,
use AskUserQuestion with 1–2 specific, concrete questions.

Available questions (choose only the relevant ones):
- "Which file or component specifically?"
- "What output format do you prefer? (code, list, explanation, JSON)"
- "Do you have an example of ideal input/output?"
- "Are there technical constraints? (language, performance, compatibility)"
- "How will you know the result is correct?"

Rule: atomic, self-contained tasks → respond directly, zero questions.
Clear examples (no questions): "change icon color to red", "list the files", "what is a mutex"
Vague examples (ask): "improve the code", "fix", "refactor auth"
